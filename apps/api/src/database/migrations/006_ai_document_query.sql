-- Schema consolidado en 001_initial_schema.sql
-- Las tablas para IA documental (document_chunks, document_embeddings,
-- document_query_history) se crearon directamente en la migracion inicial.
-- No se requieren cambios adicionales en esta migracion.
SELECT 1;